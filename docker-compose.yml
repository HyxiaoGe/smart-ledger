services:
  app:
    build: .
    container_name: smart-ledger-app
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    volumes:
      - .:/app
      - node_modules_cache:/app/node_modules
    command: npm run dev

volumes:
  node_modules_cache:

